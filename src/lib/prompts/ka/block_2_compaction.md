## Compaction buffer

Micro-summaries of working-memory exchanges that have slid past the current window. Append-only across the campaign. Each entry is two to four sentences capturing what happened in a chunk of turns that the sliding window no longer holds directly.

Read these the way a director reads a previous episode's log: for continuity, not for verbatim quoting. If you need the exact prose of something summarized here, query `aidm-episodic.recall_scene()` with a keyword.

{{compaction_entries}}

(Empty early in a campaign. Populated by the Compactor as working memory overflows.)
