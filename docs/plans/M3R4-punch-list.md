# M3R4 — The punch list: residual work between M3 and M4

**Status:** draft for user ratification (compiled 2026-08-06 from a full-repo + memory harvest merged with the M3-close ledger). M3 closed 2026-08-05 (60c799b) on its recorded exit condition; this plan is everything still owed, organized by what unblocks it. Nothing here relitigates a closed §13 decision; bucket D exists so milestone-owned work stops reading as forgotten residue.

---

## Bucket 0 — Closed since recording (the zombie-kill list)

Items still cited as open somewhere, actually done — recorded here so no future audit resurrects them:

- The soak retro's 6 within-turn read-frac failures + the turn-24 ceiling breach — ONE defect (the trailer `tool_choice` cache re-key), fixed and wire-pinned in 60c799b; the multi-call cost model absorbed the ceiling.
- Douga latency targets + thinking allowance — re-baselined (60c799b).
- The live-sweep-before-prompt-push rule — ratified 2026-08-05, in CLAUDE.md.
- The C9 douga-triage floor (M1-soak carry) and M2-drift-soak's "meter per-attempt, assert per-attempt" — landed with C9 / the soak harness.
- The recap event-mix miss — harness cause (`openSession` without `resume: true`) fixed in 60c799b; recap firing stays a watch item on the next soak, not a defect.
- M1-soak's aspirational latency markers — superseded by the measured re-baselines.
- The M2R6 "effort↔tier cache ruling pending" — superseded by the 2026-08-01 flat-high ruling.
- Multi-provider (three native KAs, per-campaign provider) — SUPERSEDED per A7; Anthropic-only for generation stands and the project memory is stamped to say so (2026-08-06), so M1.5's dispatch-extraction FU-D is moot too.
- ElevenLabs CLI access — done (v0.5.6, authed and on the roster, 2026-08-06); what remains under A6 is only the balance glance the CLI cannot serve.

## Bucket A — User rulings / business calls (updated 2026-08-06 with his answers)

- **A1 · Douga effort — RESOLVED: keep flat-high; the cache supersedes.** The user's own catch: effort is a cache-key ingredient, so douga→low would break the shared prefix lineage twice per interleaved douga turn (~$0.30 of re-writes at Sonnet, ~$1.00 at Fable, to save ~$0.06 of thinking) — decisively backwards. The latency lever moves to A2: a conte-side (Block 4, never cached) douga nudge line, tested blind.
- **A2 · The blind A/B — APPROVED, three arms (~$4.60):** sakuga high-vs-xhigh (2 scenes, cold, Fable — explicit user approval satisfies the no-Fable rule, price on record) + douga with/without the conte "no deliberation" nudge (Sonnet — a mechanism test, thinking tokens and TTFT are the measures). Output: blind pairs presented to the USER (key sealed), judge scores + token/latency metrics beside them — his ear is the final judge, per the original ruling.
- **A3 · Drift-soak retro renaming — DONE (ruled "rename it" 2026-08-06):** docs/retros/M2-drift-soak.md → M2-drift-soak-24turn.md, every reference updated including drift-soak.ts's report path (a future run writes the honest name, not a resurrection of the old one).
- **A4 · Deus Versus hard-line repair — CLOSED, won't-do (his word 2026-08-06):** all existing campaigns are depreciable testbeds; no repairs.
- **A5 · §13.5 playtester bridge — DIRECTION RATIFIED (2026-08-06):** a couple of testers, default $20 cap, per-tester caps on an admin-facing surface. Plan drafted: docs/plans/M3R5-playtester-bridge.md (three commits: allowlist+cap enforcement at the choke point, the admin page, tester-experience guards incl. Fable off by default). Awaiting plan ratification + one open question (show testers their own allowance meter — recommended yes).
- **A6 · ElevenLabs calibration — RESOLVED (dashboard read 2026-08-06: $15.78).** Blended rate ≈ $0.0117/1k chars over 359,927 ledger chars; a listened scene costs 5–8¢; the listen button is budget noise. Recorded in the budget memory.
- **A7 · Multi-provider — RESOLVED: superseded (his word, 2026-08-06).** v4 fossil; Anthropic-only for generation stands; Google survives only as an M5 media bake-off candidate, where Seedance 2 / GPT-image are already noted as the field to beat. Memory stamped.
- **A8 · Seed-window red — RULED "keep the FAIL" (2026-08-06)**, with the engine repair pulled forward: the user asked "when are we getting the engine repair?" — answer: diagnosis dispatched same day (the §7.6 dailies push/abandon discipline + the never-chosen mention verdict, one recon), repair builds as its own commit as soon as the M3R4 push train lands. Original framing kept below for the record.
- **A8 (original) · Seed-window red: hard gate or advisory?** B1's real seed-integrity suite found the N=50 soak's Director let 13 of 19 seeds expire un-pushed and paid 4 of 6 late (see B3's two new repair rows). Until the engine repair lands, any evals run pointed at that campaign is honestly red. Rule: does expired-unpaid stay a FAIL (the gate keeps its teeth, runs stay red until the repair) or drop to advisory (reported, never failing) in the interim? Recommendation: keep it a FAIL — plain `--ci` is unaffected, and a red instrument pointed at real rot is the instrument working. Consequence to know either way (audit RISK-1): seed-integrity is deterministic, so bare `pnpm evals` on the dev machine now exits 1 on this known finding — until the repair lands, a live sweep's verdict is read per-suite ("red only on seed-integrity's recorded finding" = the sweep's OTHER suites gate the push).

## Bucket B — The R4 punch-list arc (buildable now; three commits, full cadence each)

**B1 — Gates get teeth.**
- Build the real seed-integrity suite (§10.5 payoff windows + organic-detection recall over the soaked campaign; the scaffold's skip text has implied machinery that never existed since M3-C5).
- Harden renderer-efficacy: per-axis reps/majority + three-way verdicts (4 runs produced 4 distinct single-sample failure modes incl. a control-arm drift and a 529-as-FAIL; same repair its siblings channel-routing and control-key received).
- Re-baseline genga/sakuga latency targets from the soak's measured distributions (73 of 101 waste-flags are unactionable against dead letters).

**B2 — Hygiene sweep (one commit).**
- CLAUDE.md "traced trio" → the four traced calls (+ the same phrase in control-key.ts:29).
- cache-gauge.ts:425 stale "pending migration" note (the phase table was fixed in the writers'-room close).
- `soak50.log` out of the repo root (gitignore + git rm; the retro is the record).
- Probe-call phase attribution: 46 phase-NULL rows/run — thread `phase` through the probe callers that lack it.
- Prewarm→KA posture parity pinned in code (empirically healthy on the soak; the comment currently says "unproven").
- Memory reconciliations (multi-provider per A7's answer; M1.5's dispatch-extraction FU-D marked superseded if A7 confirms Anthropic-only).

**B3 — Carried repairs (one commit).**
- ElevenLabs ledger overcount — LANDED (B3, 2026-08-06), hypothesis CORRECTED by the fix: not chunk-rows — the route metered on response-headers-arrival, so the listen button's prefetch-and-abandon (eager `preload="auto"` on segment k+1, discarded on stop) billed full segments never delivered. Now exactly one row per CLEAN completion; aborts row nothing and warn the would-be chars (an honest residual undercount — ElevenLabs bills partial transfers we no longer row; the dashboard stays character truth for that residue). Pinned with stream tests.
- Red Sash class (M1-soak carry): dialogue-embedded world claims miss the WORLD_BUILDING gate — widen the extractor's reach, pin with the original miss as fixture.
- Semantic alias resolution — LANDED (B3, 2026-08-06), row was STALE: the semantic tier already existed (janitor's pairLikelySame IS a traced semantic judge); the real gap was candidate-filter recall — names alone were embedded, so "the Red Sash syndicate" / "the dockworkers' syndicate" never reached the judge. Fixed with a second name+block-head vector offered only for name-rejected pairs, spending only probe budget the name tier left over (no new threshold invented).
- Entity block-quote correction fallback — LANDED (B3, 2026-08-06): quote path now mirrors the critical-fact idiom (hint floor ≥12 chars, all-matches uniqueness, quote-it on 0 or >1; ambiguous line matches supersede nothing). Name binding stays exempt (identity-key equality).
- voice_cards research starvation — LANDED (B3, 2026-08-06), NOT closed by M3R3 after all: research.ts's `wikiOnly` filter (correct for the speaker-keyed quote block) also starved the card producer of the characters-topic harvest, so Level-B profiles shipped zero cards silently. Fixed with a capped unattributed cast corpus (3 pages × 1,500 chars) + a coverageGates DEFECT for cast-material-present-but-zero-cards (legacy cached profiles exempt by optional inputs).
- Eval-suite spend files as play (found by B2's phase audit): channel-routing.ts and authorship-detection.ts pass `turnNumber`, so their probe/judgment rows land in the "turn" bucket — and `campaignId` is `on delete set null`, so the rows outlive their throwaway campaigns and pollute the global per-turn baselines forever. Same defect class as the harness fix; sweep ALL eval callers (judgment too) to an honest phase.
- Extractor entity-name instability (found 2026-08-06 when case (b) was de-vacuized): identical EXTRACTOR_SYSTEM bytes returned 5 resolved entity_names on one run and 0 on the next, at the probe tier — the battle-scream's third parties (the mother) resolve intermittently, so dossiers accrue by coin flip on expressive inputs. authorship-detection case (b) now REQUIRES the resolution and will red intermittently until repaired — a known measured red, same reading as seed-integrity's. Repair candidates (pick with measurement, not another single run): judgment-tier for the entity pass of extraction (per-turn cost delta must be priced first), or reps-with-merge inside ingestion. A prompt iteration ("file the vow under the sister") scored 1/2 vs baseline 0/2 — plausibly better, unproven, reverted; re-landing it wants reps.
- Seed-window discipline (found by B1's real seed-integrity gate, N=50 data): the Director paid 4 of 6 settled seeds LATE and left 13 of 19 open past their window — `adjust_window` was never used once, and no seed was ever deliberately abandoned. The §7.6 dailies pass needs to actually exercise push/abandon on expiring seeds. Engine change: needs its own plan row (named failure mode: seeds rot silently and payoffs land stale; pillar §7.6/§10.5) before code.
- Organic promotion exercised-but-never-chosen (same B1 run; diagnosis sharpened by the audit): the sweep surfaced 197 organic candidates across 19 seeds and the §7.6 adjudicator promoted ZERO — but the wire is NOT dead: the promotion path exists (adjudication.ts records a mention on verdict "mention") and the adjudicator demonstrably ran (4 payoffs resolved by it). It just never selects "mention." That's judgment calibration — the verdict prompt/bar — not plumbing: diagnose why the mention verdict is unreachable in practice, then plan-row discipline if the fix is an engine change.

**B5 — The theme toggle (RATIFIED 2026-08-06, small feature, its own commit).** The light theme has never rendered: Tailwind v4 hoists the media-nested `@theme` block, so the dark values always win regardless of OS preference — the user didn't know a light theme existed. Ruling: (b)+(c) — fix light mode AND expose a toggle in the campaign settings menu; dark by default ("different people have different eyes and prefer different things, esp for reading a lot"). Shape: dark becomes the honest CSS BASE, light becomes a `data-theme="light"` override on the root; preference stored player-scoped (players.profile jsonb — cross-campaign taste, layer 10) so it survives devices; the settings menu gains the toggle beside the other machinery preferences. C10 both-themes verification becomes REAL with this commit (and applies to every surface).

**B4 — The stinger (small feature, its own commit).** `stinger_allowed` has sat PENDING in premise.ts since display-grammar: needs the SZ writer (one conductor beat) + the `composeStinger` reader. Whole-shape rule: ship writer and reader together. (Chip-skinning's reserved field stays pending — it is data-gated on chips accumulating usage, not buildable now.)
  - BUILT 2026-08-06, audited (1 low + 2 nits, fixed). Two yokoku-parity postures named by the audit for a future directional call, both deliberate matches to the yokoku's existing behavior: (1) the recap is the stinger's ONLY reader — a SKIPped/failed recap or a pilot next-sitting drops the plant, and the KA never sees the stinger in Block 3, so a next-turn contradiction is possible; (2) an idle-timeout close composes a stinger the player never sees (one extra composer call per idle close on granted premises). If either bothers in play, they're yokoku-and-stinger repairs together, not stinger-only.

## The §7.6 repair plan (diagnosed 2026-08-06; two commits, R-1 then R-2)

Diagnosis (full report in session record): ONE root cause under both B3 seed rows — `callStructured` lets the SDK zod-validate inside stream accumulation, so a schema violation throws in the transport catch BEFORE `recordModelCall` and BEFORE the manual corrective-retry block. The retry has been dead code for every schema violation since it was written; every failed call is billed-but-unmetered. Soak evidence: 13/18 adjudication calls died on an UPPERCASE-prompt vs lowercase-z.enum mismatch (nested enums aren't grammar-enforced — the codebase already records this); the Pacer hit the same class at turn 50; 150/197 organic candidates were never adjudicated at all, and the ~47 that were saw 149-char fragments of ~3,525-char scenes under a "say NONE rather than hedge" instruction — NONE was correct given the evidence. Cadence exonerated (cycles every ~3 turns; rotted seeds sat OVERDUE-marked 2–10 consecutive cycles). The "4 late payoffs" are a metric artifact: settlement only happens on cycle turns, max structural lateness = interval−1.

- **R-1 (substrate; named failure mode: dead retry + unmetered spend at the choke point; pillar: the traced-calls discipline + §0.9):** calls.ts stops SDK auto-parse (plain json_schema output format), restoring the manual parse + one-corrective-retry + metering on EVERY outcome; verdict/phase enums taken as lean strings and normalized engine-side in the existing clamps (unknown → none; doctrine already stated at types/direction.ts:641). Unit tests: violation → retry fires once + meter row; double violation → throws but both metered.
- **R-2 (§7.6 surface; named failure mode: seeds rot silently, mentions unreachable; pillar §7.6/§10.5):** adjudication evidence widens to the actual narration of the turns a seed's candidates name; the mention question separates from the payoff/conflict slot; the dossier stops truncating descriptions (or bestMatch strips the ellipsis); settleSeed notes logged, AdjudicationResult counts persisted; a CLOSING (expiring-soon) marker from the existing window + DIRECTOR_MAX_INTERVAL so the Director is asked to push BEFORE the promise breaks; seed-integrity grades lateness with cycle tolerance (interval−1 is structure, not rot — the 13-rotted finding stands untouched).
- **R-2 scope additions (found during R-1, 2026-08-06):** (a) director.ts:1017's startup prompt says "set phase to Setup" against lowercase PacerPhase, nested in StartupPlan.arc, and directorStartup has NO catch — latent campaign-open-fatal if the model ever follows the prose's casing (soaks survived because models usually emit lowercase; R-1's restored retry now also covers it, but the casing + a catch belong to R-2); (b) ingest.ts ExtractedFact.posture prose says "FLAG" vs enum "flag" — same class; (c) a 15th soak failure the diagnosis missed: a whole Director cycle died turn 31 on arc_plan.budget.unit (ArcBudget) — same root cause, covered by R-1's restored retry, listed so the fresh-soak verification knows to look. Also doctrinal: the SDK demotes EVERY enum to an advisory description at every nesting level (not just nested ones) — types/direction.ts:650's "grammar-native" claim was false and is corrected; every judgment-schema enum is prompt-honored, not grammar-enforced.
- **What no fix can prove without a fresh soak:** that the Director elects push/abandon once offered a live path; that widened evidence flips MENTION to sometimes-chosen; whether ORGANIC_CANDIDATE_THRESHOLD=0.55 discriminates once the ledger isn't saturated. Deterministic tests prove reachability only; the enum clamp at real grammar compilers rides the live sweep's schema-grammar suite.

## Bucket C — Measurement-blocked (the funded-soak / Fable era)

- not-another-anime register wobble (surfaced by the 2026-08-06 live sweep + stability reads): `temporal_structure_b1_sorayori` failed 2 of 4 runs that day and `density_b1_aria` once — band structure always judged true, register judged "generic" on the misses, each already under majority-of-3. Not diff-correlated (no narration bytes moved in B3). The signal: band-1 register specificity for quiet ensemble shows wobbles at the model/judge boundary. Needs funded reps to separate "narration genuinely generic on these registers" (an exemplar/charter repair) from "judge variance on a subtle call" (an instrument repair). Until then: a single-run naa red on a b1 register case is a known wobble, not a push-blocking regression — unless the diff touched narration or exemplar bytes.

- Sakuga thinking allowance 16k: known over-model; moves only when a Fable-served sakuga turn is measured.
- Control-key SAMPLES=2: bigger N when soak-scale budget exists.
- Director cycle fan-out (1–11 judgment calls, 10.7% of soak spend): measure why it varies before any cap.
- Prefix-fix verification at scale: free with the next soak.
- The N=100 soak: §10.3's upper letter — optional (M3 closed at the user-ratified N=50), natural vehicle for everything above in this bucket.
- OP_COMMAND one-shot design (runtime.ts/booth.ts): design-blocked, not measurement-blocked — needs its own G1-side plan row before code (ledger rule).

## Bucket D — Milestone-owned (the blueprint's schedule; listed so nothing reads as dropped)

- **M4:** studio view §13.4 (contract editing incl. the Bible's remaining archive-read intensity fields; taste display; active-premise editing) · deep rewind (epoch dissolution, compacted-beat resurrection) · hybrid per-component assembly · semantic retrieval runtime + `search_memory` + `turns.summary` · Director reads `turns.flags` · cost-dashboard aggregate · M1-closure Phase 8 tickets · Group-B trust labels (director_personality/author_voice grounding).
- **M5:** billing substrate · media module (g2 dispatch point, portraits, model_calls media tiers) · CombatAgent/ScaleSelector tuning · Compiled Campaign, shelf polish, abuse/limits.
- **M6+:** douga-routing experiment (§13.6, parked) · sound department (post-v5) · long-play retro, v6 go/no-go.
- **Data-gated:** chip-skinning (needs chip usage data).

## Order

B2 (hygiene, fast) → B1 (the gates) → B3 (carried repairs) → B4 (stinger). A-bucket rulings land whenever given and slot into whichever commit is open. C waits for its measurements; D waits for its milestones. Every commit: full cadence — build → Opus review → fix → bare gate (+ live sweep when prompts move) → push → CI → Railway.
