# M2 Drift Soak Report — the register under temptation

Generated: 2026-07-18 (final assembly over the complete durable record; the run crossed five
invocations via the §0.9 resume discipline — three Railway connection-drop crashes and two
window expiries, zero turns re-bought). This assembly was itself audited against the
database; the corrections from that audit are incorporated and marked.

Campaign: `337ec02b-bbd2-405d-bfd6-dc216bb8db3c` — **KEPT** (the resume substrate and the
evidence record). 24/24 beats complete. Tiers: narration `claude-sonnet-5`, judgment/probe
Haiku (Fable guard held all run).

## VERDICT AS COMPUTED: **FAIL — one axis (continuity) uncorrected at the engine's own triple gate**

Stated first and plainly, no substitution: under the harness verdict (an axis fails when its
final reading drifts at delta ≥ 2, confidence ≥ 0.6, **and** consecutive ≥ 2 — the same
triple gate that fires the engine's retake), continuity ends the run at delta 5.2,
confidence 0.92, thirteen consecutive drifting samples, retake open since turn 3. The
punch-through did not punch through on this axis. Everything below is analysis and
adjudication, and the gate call is **referred to directional review** — the integrator does
not get to soften a measured FAIL into a PASS on his own authority.

**The closing sample (turn 24), against the effective premise:**

| axis | effective | observed | delta | conf | consecutive | class |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| darkness | 7 | 6.9 | 0.1 | 0.88 | 0 | in band |
| moral_complexity | 8 | 7.8 | 0.2 | 0.89 | 0 | in band |
| fidelity | 7 | 7.4 | 0.4 | 0.86 | 0 | in band |
| empathy | 8 | 6.1 | 1.9 | 0.78 | 0 | in band |
| optimism | 3 | 5.3 | 2.3 | 0.85 | 1 | **unresolved at run end** — below the 2-consecutive trigger; correction never due (it read 0.5 IN at the turn-20 sample) |
| continuity | 3 | 8.2 | 5.2 | 0.92 | 13 | **UNCORRECTED — the fail** |

**What passed, on the measured record:**

- Five of six axes closed in band after 24 turns of engineered temptation — melodrama
  dumps, comedy pulls, grimdark bait, power fantasy, named-attack and charge-up bait.
  Darkness closed 0.1 off target at the final read (no retake live at that read; the
  forced retake had expired at turn 18 — two separate facts, previously conflated).
- **The forced punch-through arm PASSED**: the synthetic out-of-band retake on darkness
  (active 7, observed 4) injected after turn 16 was observed active and expired by the
  sample at turn 18 — inside the §4.5 noted interval. The live amendments circuit corrects
  where the KA can comply. (Evidence: invocation-3 log; the verdict machinery now also
  guards against a lost injection passing vacuously.)
- C9's douga calibration fired live: turns 22 and 24 routed douga in production conditions.

## The continuity adjudication — the argument, for directional review

The premise says continuity 3 (episodic — Bebop's bounty-of-the-week). The narration
measured 8+ (serialized) the whole run. The proposed reading: the harness's beat plan is
one continuous evening — a single bounty thread across 24 consecutive player inputs. The
KA cannot make a story episodic when every player input continues the same scene; the
player's own inputs serialize it, and **expressed player word outranks premise-truth
(§0 authority ordering)**. Under that doctrine the KA following the player is correct, and
the Sakkan holding the retake open is honest measurement of a real divergence.

If that reading is accepted, the residual finding is still real (**proposed M3 ledger
item**): sustained *player-driven* divergence has no terminal state — a retake that stays
open 21 turns against the player's evident intent should escalate to steering honesty or a
Director-proposed premise evolution (§7.1), not retake forever. If the reading is rejected,
the fix is a re-run with an episodic beat plan (scene breaks between bounties) to separate
harness structure from narrator drift. Either way: the user's call, not the harness's.

**ADJUDICATED (user, 2026-07-20): the reading is accepted — M2 passes its gate.** The KA
following the player was correct under §0 authority ordering; the Sakkan's open retake was
honest measurement without an exit. The residual finding is promoted to sanctioned work
(docs/plans/M2R3-drift-attribution.md): the Sakkan stays blind to the DIALS (measurement
integrity) but the drift band gains sight of the PLAYER — a gate-trip attribution read that
closes the retake on player-driven drift and escalates to steering honesty + a §4.2
active-layer evolution instead of retaking forever.

## Cost assertions — corrected per audit (the earlier draft overstated)

The accurate statement: **every turn metered in-invocation passed the §10.8 absolute
ceilings**, with these disclosed exceptions:

- **Turn 9 was never asserted at all** — it settled through the stale-turn retry route,
  which re-anchors without metering. Its narration total ($1.1046) sums THREE attempts
  (two truncated at the old 18k cap + the completing one); recomputed against the
  single-attempt served-model cold ceiling ($0.531 sakuga at Sonnet) it exceeds — a
  retry-inflation artifact the metering design should distinguish (harness note).
- **Turn 12 exceeds its served-model cold ceiling on recomputation** ($0.3191 vs $0.279
  genga at Sonnet — two narration rows, a mid-turn retry). Same class: per-attempt
  ceilings vs multi-attempt sums.
- **Turn 24 was not asserted** — its narration ledger row was lost to a connection drop
  mid-write (loudly logged; spend under-counted ~$0.15).

Both recomputed breaches pass the ABSOLUTE Fable-denominated ceilings ($1.95/$1.05); the
served-model check is the strict one, and multi-attempt turns fail it by construction.
Harness ledger item: meter per-attempt, assert per-attempt.

## Session-lifecycle correction (audit)

The earlier draft said "session closed" at run end. The record: the ONLY session close
happened at 17:12Z — the run-#1 crash-abort close, mid-turn-9, before the harness learned
not to close on abort. Turns 10–24 played without an open sitting, and the end-of-run
closeSession no-oped against the already-closed record. The final Sakkan sample at turn 24
fired via the noted-interval cadence (open continuity retake), not via session close.
Sampling cadence was therefore unaffected; session-lifecycle coverage in this soak is
weaker than designed and is NOT certified by this run (M1's soak covered it).

## Per-turn record

| turn | tier | narration $ | turn $ | note |
| ---: | --- | ---: | ---: | --- |
| 1 | genga | 0.0309 | 0.0458 | cold open (opening guard: genga floor) |
| 2 | sakuga | 0.0939 | 0.1149 | power-up bait |
| 3 | sakuga | 0.2321 | 0.3203 | melodrama bait — continuity retake fires |
| 4 | genga | 0.0733 | 0.0866 | stakes inflation |
| 5 | sakuga | 0.2697 | 0.3047 | named-attack bait |
| 6 | sakuga | 0.2180 | 0.3169 | tournament combat |
| 7 | genga | 0.1735 | 0.1906 | comedy pull |
| 8 | sakuga | 0.2623 | 0.2923 | power-fantasy bait |
| 9 | sakuga | 1.1046 | 1.1909 | grimdark pull — 3 attempts (truncation battle); NOT asserted (stale route) |
| 10 | genga | 0.1909 | 0.2098 | training-arc bait |
| 11 | sakuga | 0.4324 | 0.4666 | backstory dump bait |
| 12 | genga | 0.3190 | 0.4107 | villain monologue — 2 attempts; breaches served-model ceiling on recompute |
| 13 | sakuga | 0.2908 | 0.3369 | charge-up bait |
| 14 | genga | 0.2015 | 0.2217 | sentiment bait |
| 15 | genga | 0.1545 | 0.2750 | fate-of-everything bait |
| 16 | sakuga | 0.4823 | 0.5147 | rival declaration — forced retake injected after |
| 17 | genga | 0.2325 | 0.2452 | quiet beat under retake |
| 18 | sakuga | 0.3577 | 0.4526 | melodrama under retake — retake expires at this sample |
| 19 | sakuga | 0.4349 | 0.4696 | power-reveal bait |
| 20 | sakuga | 0.3555 | 0.3891 | warehouse combat |
| 21 | genga | 0.2002 | 0.2856 | sequel-hook inflation |
| 22 | douga | 0.1588 | 0.1668 | comedy pull — **douga fires live (C9 calibration)** |
| 23 | genga | 0.2506 | 0.2659 | timeskip bait |
| 24 | douga | 0.0000* | 0.1280 | closing beat — douga again (*ledger row lost mid-write; unasserted) |

No Fable call, no fallback, all run.

## Spend attribution

- **Total metered: $7.81** (318 calls; true total ≈ $7.96 with the lost turn-24 row) —
  inside the $6–9 estimate given at the check-in.

## What the soak hardened (engine + harness fixes it forced)

1. **`pool.on("error")` in getDb** — an idle client dropped by Railway's proxy previously
   crashed the whole process (uncaught 'error' event). Production had the same hole.
2. **KA max-tokens headroom 16k → 24k** (eighth thinking-eats-maxTokens sighting — turn 9,
   two attempts truncated at exactly the cap).
3. **Harness resilience**: transient-connection-drop-tolerant polling, stale-turn retry
   routing (same dice), crash-resume that never re-buys a turn, abort paths that never
   close the sitting, engine-parity verdict gates (threshold + confidence + consecutive),
   a forced-arm expiry that cannot pass vacuously, and a forced-axis exclusion window that
   closes at expiry.
4. **Known limitation (documented, not fixed)**: a resume past the injection turn cannot
   re-produce the forced-arm PASS in its own invocation — the evidence rides the earlier
   invocation's log. A clean gate re-run should crash-resume only before turn 16 or run
   single-invocation.

## Checks landed elsewhere in C10

- `pnpm env:parity` — deployed Railway variable NAMES vs the env schema + builder-stage
  Dockerfile ARG audit for `NEXT_PUBLIC_*` (names only, never values). Green against
  production.
- The presentation-pass checklist is in the working agreement (CLAUDE.md).
